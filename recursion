#include <iostream>
using namespace std;

int fact (int n)
{
	//for, dowhile, while
	if (n==0 || n== 1)
	{
		return 1;
	}
	else
	{
		return n * fact(n-1);
		//5*4*3*2*1=120
	}
}

int main()
{
	int n;
	cout<<"Dame un numero"<<endl;
	cin>>n;
	cout<<fact(n);
}
